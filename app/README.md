# Qt visualization shell — coming in v0.3

Planned modules:

- `FoveaLens` — Qt Quick 3D scene view
- Confidence-weighted bounding boxes
- Prediction ghost overlays
- Live FPS / latency HUD

Build with:

```bash
cmake -S . -B build -DFOVEA_ENABLE_QT=ON
```
